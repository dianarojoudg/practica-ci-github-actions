name: CI Pipeline - Práctica Universitaria

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4
      
      - name: Simular instalación de dependencias
        run: echo "✅ Dependencias instaladas correctamente" > resultado.txt
      
      - name: Ejecutar pruebas simuladas
        run: |
          echo "🧪 Ejecutando pruebas unitarias..."
          echo "Test 1: Passed ✅"
          echo "Test 2: Passed ✅"
          echo "Cobertura de código: 95%"
      
      - name: Mostrar resultado
        run: cat resultado.txt
